import java.util.Scanner;

public class MazeSolver {
	public static String path;
	public static void findPath()
	{
		Scanner sc = new Scanner(System.in);
	}
	public static void f(int r, int c, String path)
	{
		// recursive function
		// call up down left right and mark 
		// output path in solve
		/**
		 * if out of bounds or invalid return
		 * if at end update path and return
		 * grid[r][c] = "#"
		 * f(r-1, c, path + "U")
		 * r( for the rest down right and left
		 */
	}
	public static String solve()
	{
		//read input
		//f(on the start)
		return path;
	}
}
